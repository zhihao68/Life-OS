import React, { useState } from 'react';
import { Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useLifeOS } from '../store/LifeOSContext';
import { Card, colors, Header, Segment, shadow } from '../components/ui';
export function NotesScreen() { const { state } = useLifeOS(); const [tab, setTab] = useState(0); const [query, setQuery] = useState(''); const filtered = state.notes.filter((note) => !query || `${note.title} ${note.tags.join(' ')} ${note.markdown}`.toLowerCase().includes(query.toLowerCase())); return <><Header title="笔记" subtitle="你的第二大脑" icon="ellipsis-horizontal" /><View style={styles.search}><Ionicons name="search-outline" size={17} color={colors.muted} /><TextInput value={query} onChangeText={setQuery} placeholder="搜索笔记、标签或内容" placeholderTextColor="#A2A9B7" style={styles.searchText} /></View><Segment values={['全部', '文件夹', '标签']} active={tab} onChange={setTab} /><Text style={styles.label}>置顶笔记</Text><Card>{filtered.filter((note) => note.pinned).map((note) => <NoteRow note={note} key={note.id} />)}</Card><Text style={[styles.label, { marginTop: 21 }]}>知识关联</Text><Card><View style={styles.graphHeader}><View style={styles.graphIcon}><Ionicons name="git-network-outline" size={18} color={colors.purple} /></View><View style={{ flex: 1 }}><Text style={styles.graphTitle}>论文实验记录</Text><Text style={styles.graphHint}>2 个反向链接 · 1 个关联任务</Text></View><Ionicons name="chevron-forward" size={18} color="#AAB0BC" /></View><View style={styles.linkPills}><Text style={styles.pill}>[[UASB实验数据]]</Text><Text style={styles.pill}>#论文</Text><Text style={styles.pill}>关联任务</Text></View></Card><Text style={[styles.label, { marginTop: 21 }]}>最近编辑</Text><Card>{filtered.filter((note) => !note.pinned).concat(filtered.filter((note) => note.pinned)).slice(0, 4).map((note) => <NoteRow note={note} key={`${note.id}-recent`} />)}</Card><Pressable style={styles.fab} accessibilityLabel="新建笔记"><Ionicons name="add" size={25} color="#fff" /></Pressable></>; }
function NoteRow({ note }: { note: { id: string; title: string; tags: string[]; updatedAt: string } }) { return <Pressable style={styles.row}><View style={styles.thumb}><Ionicons name="document-text" size={19} color={colors.purple} /></View><View style={{ flex: 1 }}><Text style={styles.title}>{note.title}</Text><Text style={styles.hint}>{note.updatedAt} · {note.tags.map((tag) => `#${tag}`).join(' ')}</Text></View><Ionicons name="chevron-forward" size={17} color="#AAB0BC" /></Pressable>; }
const styles = StyleSheet.create({ search: { height: 45, marginBottom: 14, paddingHorizontal: 13, borderRadius: 14, backgroundColor: colors.white, borderWidth: 1, borderColor: colors.line, flexDirection: 'row', alignItems: 'center', gap: 8 }, searchText: { flex: 1, fontSize: 12, color: colors.ink }, label: { marginBottom: 10, color: '#2A3447', fontSize: 14, fontWeight: '700' }, row: { minHeight: 67, borderBottomWidth: 1, borderBottomColor: '#F0F2F6', flexDirection: 'row', alignItems: 'center', gap: 11 }, thumb: { width: 44, height: 44, borderRadius: 12, backgroundColor: colors.purpleSoft, alignItems: 'center', justifyContent: 'center' }, title: { color: '#2E394D', fontSize: 13, fontWeight: '600' }, hint: { marginTop: 5, color: colors.muted, fontSize: 10 }, graphHeader: { flexDirection: 'row', alignItems: 'center', gap: 10 }, graphIcon: { width: 35, height: 35, borderRadius: 11, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.purpleSoft }, graphTitle: { color: colors.ink, fontSize: 13, fontWeight: '700' }, graphHint: { marginTop: 4, color: colors.muted, fontSize: 10 }, linkPills: { marginTop: 14, flexDirection: 'row', gap: 7, flexWrap: 'wrap' }, pill: { paddingHorizontal: 9, paddingVertical: 6, borderRadius: 9, backgroundColor: '#F3F5FA', color: '#6F7890', fontSize: 10 }, fab: { position: 'absolute', right: 22, bottom: 93, width: 49, height: 49, borderRadius: 25, backgroundColor: colors.purple, alignItems: 'center', justifyContent: 'center', ...shadow } });
